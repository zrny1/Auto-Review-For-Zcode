# process

## 本轮需求/背景

用户提出（2026-09-18）：

1. 检查 project_wiki、README 等文档是否需要更新，需要则更新
2. 确认文档和提交历史中是否含有敏感信息
3. 说明如何将 project_wiki 转换为 GitHub 上的 Wiki 文档

## 预期修改计划

- 对 0.2.4/0.2.5 两轮变更后未同步的 wiki 文档做时效性补齐（决策管线防护、provider
  三源合并流程图、环境变量表、已知坑、接入点选型结论、安装缓存同步说明等）
- 敏感信息全量扫描：当前工作区（密钥/个人路径/用户名/邮箱/手机号）+ 全部 44 个提交
  的历史 diff
- GitHub Wiki 迁移方案以答复形式给出（无代码）

## 实际修改步骤

- **模块设计_决策管线.md**：流程图 ③ 全文规则层与复合逐段层的 allow 分支补齐命令
  替换抑制语义（含"单段替换继续向后扫 deny/ask"）；关键不变量 2 与"复合命令逐段设计"
  同步补齐 0.2.4 防护描述
- **功能介绍.md**："确定性优先"要点补充命令替换构造不放行说明
- **使用指南.md**：原生审批框分析显示的表述修正为精确版（自动编辑模式走升级路径
  显示、default 模式同向叠加丢弃——3.12.3 逆向结论）；GUI provider 下拉说明补
  providerName 优先显示与多源合并
- **快速开始.md**：安装节新增"更新插件须知"——本地 marketplace 运行缓存仅在 ZCode
  启动时从项目根同步，改代码需重启（含手动同步缓存的指引）
- **扩展点与接口.md**：环境变量表补 `AUTO_REVIEW_PROVIDER_CONFIG`；"接入其他事件"节
  新增 PermissionRequest vs PreToolUse 选型结论（逆向实证要点）；"已知坑"表新增两行
  （缓存仅启动时同步、PS5.1 无 BOM UTF-8 按 ANSI 解析）
- **技术栈与项目结构.md**：provider.js 职责行更新为三源合并统一表
- **模块运行流程图.md**：provider 解析顺序 mermaid 重绘为三源合并流程（修正过程中
  发现并修复节点 ID 冲突）
- **敏感信息扫描**：当前工作区 grep（sk-/tp-/32hex.16 位后缀密钥模式、C:\Users\zyb、
  D:\Soft、非作者名 zyb、邮箱、手机号）全部无命中；`git log --all -p`（44 提交）同
  模式扫描全部无命中；llm_smoke/debug 脚本确认仅 example.com 占位与运行时解析的
  baseURL，无硬编码密钥
- 根 README 与 wiki 首页版本号已在 0.2.5 周期同步（0.2.5），本轮无需变更

## 验证结果

- 文档更新后逐处复读确认表述与当前代码行为一致；mermaid 流程图节点 ID 无冲突
- 敏感信息扫描：工作区与全部提交历史均干净（密钥/个人路径/用户名/邮箱/手机号零命中）；
  仓库内路径均为 ~ 或项目内相对结构，符合"不保留个人信息与非项目文件结构"要求
- 本轮为纯文档周期，无代码变更，不涉及测试回归；版本号维持 0.2.5
